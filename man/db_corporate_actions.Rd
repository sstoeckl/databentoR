% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference.R
\name{db_corporate_actions}
\alias{db_corporate_actions}
\title{Corporate actions}
\usage{
db_corporate_actions(
  start,
  end = NULL,
  index = "event_date",
  symbols = NULL,
  stype_in = "raw_symbol",
  events = NULL,
  countries = NULL,
  exchanges = NULL,
  security_types = NULL,
  flatten = TRUE,
  pit = FALSE,
  allocate_isins = TRUE,
  compression = "zstd"
)
}
\arguments{
\item{start, end}{Request window. \code{start} is required; leaving \code{end} as
\code{NULL} asks the server to forward-fill.}

\item{index}{Column the result is ordered by, \code{"event_date"} by default.}

\item{symbols}{Character vector of symbols, a comma-separated string, or
\code{NULL} for all symbols.}

\item{stype_in}{Symbol type of \code{symbols}. The reference API accepts more
types than the historical one, among them \code{"isin"}, \code{"figi"} and
\code{"nasdaq_symbol"}, and passes the value through unvalidated.}

\item{events}{Optional event-type filter, a character vector or a
comma-separated string. See \code{\link[=db_corporate_action_events]{db_corporate_action_events()}}.}

\item{countries}{Optional country filter, a character vector or a
comma-separated string.}

\item{exchanges}{Optional exchange filter. Sent only when supplied.}

\item{security_types}{Optional security-type filter.}

\item{flatten}{Expand the nested \code{date_info}, \code{rate_info} and \code{event_info}
objects into ordinary columns.}

\item{pit}{Keep every point-in-time record. \code{FALSE}, the default, keeps
only the latest record of each event.}

\item{allocate_isins}{Ask the server to allocate ISINs.}

\item{compression}{Transfer compression, \code{"zstd"} or \code{"none"}.}
}
\value{
A tibble of corporate actions.
}
\description{
Mirrors \code{corporate_actions.get_range}.
}
\examples{
\dontrun{
db_corporate_actions(start = "2024-01-01", symbols = "AAPL")
}
}
\seealso{
Other reference:
\code{\link[=db_adjustment_factors]{db_adjustment_factors()}},
\code{\link[=db_corporate_action_enums]{db_corporate_action_enums()}},
\code{\link[=db_corporate_action_events]{db_corporate_action_events()}},
\code{\link[=db_security_master]{db_security_master()}},
\code{\link[=db_security_master_last]{db_security_master_last()}}
}
\concept{reference}
