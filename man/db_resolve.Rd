% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/symbology.R
\name{db_resolve}
\alias{db_resolve}
\title{Resolve symbols from one symbol type to another}
\usage{
db_resolve(
  dataset,
  symbols,
  stype_in = "raw_symbol",
  stype_out = "instrument_id",
  start_date,
  end_date = NULL
)
}
\arguments{
\item{dataset}{Dataset code, e.g. \code{"GLBX.MDP3"}.}

\item{symbols}{Character vector of symbols, a single comma-separated
string, or \code{NULL} for all symbols. With \code{stype_in = "instrument_id"} a
numeric vector is accepted. At most 2000 symbols per request.}

\item{stype_in}{Symbol type of \code{symbols}, see \code{\link[=db_stypes]{db_stypes()}}. Use
\code{"parent"} for product-level symbols such as \code{"ES.FUT"} or \code{"SPX.OPT"}.}

\item{stype_out}{Symbol type to resolve to, see \code{\link[=db_stypes]{db_stypes()}}.}

\item{start_date, end_date}{Resolution window, \code{start_date} required. Both
take a \code{Date} or a \code{"YYYY-MM-DD"} string; timestamps are rejected, as in
the Python client.}
}
\value{
A tibble with one row per resolved interval: \code{input_symbol},
\code{start_date} (inclusive), \code{end_date} (exclusive) and \code{symbol}. Symbols
the server resolved only partially or not at all are attached as the
\code{"partial"} and \code{"not_found"} attributes, alongside \code{"status"} and
\code{"message"}.
}
\description{
Free of charge. Mirrors \code{symbology.resolve}. Resolution is time-dependent,
which is why the answer is a set of intervals per input symbol rather than
one mapping: an instrument id is only valid for the days it was assigned.
}
\examples{
\dontrun{
db_resolve("GLBX.MDP3", "ES.FUT", stype_in = "parent",
           start_date = "2024-01-01", end_date = "2024-02-01")
}
}
\concept{symbology}
