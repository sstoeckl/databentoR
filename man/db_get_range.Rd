% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timeseries.R
\name{db_get_range}
\alias{db_get_range}
\title{Download a historical range as a tibble}
\usage{
db_get_range(
  dataset,
  start,
  end = NULL,
  symbols = NULL,
  schema = "trades",
  stype_in = "raw_symbol",
  stype_out = "instrument_id",
  limit = NULL,
  path = NULL,
  pretty_px = TRUE,
  pretty_ts = TRUE,
  map_symbols = TRUE,
  ts_type = c("POSIXct", "integer64"),
  compression = c("none", "zstd"),
  col_types = NULL
)
}
\arguments{
\item{dataset}{Dataset code, e.g. \code{"GLBX.MDP3"}.}

\item{start, end}{Request window, start inclusive and end exclusive. A
string passes through verbatim, a \code{Date} becomes a plain date, a
\code{POSIXct} an ISO-8601 UTC instant, and a number is read as nanoseconds
since the UNIX epoch. Leaving \code{end} as \code{NULL} asks the server to
forward-fill from \code{start}.}

\item{symbols}{Character vector of symbols, a single comma-separated
string, or \code{NULL} for all symbols. With \code{stype_in = "instrument_id"} a
numeric vector is accepted. At most 2000 symbols per request.}

\item{schema}{Schema name, see \code{\link[=db_schemas]{db_schemas()}}.}

\item{stype_in}{Symbol type of \code{symbols}, see \code{\link[=db_stypes]{db_stypes()}}. Use
\code{"parent"} for product-level symbols such as \code{"ES.FUT"} or \code{"SPX.OPT"}.}

\item{stype_out}{Symbol type of the output, see \code{\link[=db_stypes]{db_stypes()}}. The API
resolves to \code{"instrument_id"} from every input type, and to
\code{"raw_symbol"} only from \code{"instrument_id"}.}

\item{limit}{Optional cap on the number of records.}

\item{path}{Optional file path. When given, the result is also written
there as parquet, and the tibble is returned invisibly.}

\item{pretty_px}{Ask the server for decimal prices instead of fixed-point
integers scaled by 1e-9.}

\item{pretty_ts}{Ask the server for ISO-8601 timestamps instead of
nanosecond counts. With \code{FALSE} the timestamp columns come back as
character, because a nanosecond count does not fit an R numeric type
without loss.}

\item{map_symbols}{Ask the server to append a \code{symbol} column to every
record.}

\item{ts_type}{How timestamps are represented in R. \code{"POSIXct"}, the
default, is convenient but stores seconds as a double, so on modern dates
it resolves to roughly a quarter of a microsecond rather than to the
nanosecond. \code{"integer64"} keeps the exact nanosecond count (it requires
the \code{bit64} package and forces \code{pretty_ts = FALSE}), which is what you
want for book reconstruction or any latency work.}

\item{compression}{Transfer compression, \code{"none"} or \code{"zstd"}. \code{"zstd"}
is worth it for large pulls and is decompressed transparently.}

\item{col_types}{Optional arrow schema overriding the column types
databentoR would assign.}
}
\value{
A tibble, invisibly when \code{path} is given.
}
\description{
Mirrors \code{timeseries.get_range}. Where the Python client always asks for the
binary DBN encoding and decodes it locally, databentoR asks the API for the
CSV encoding and reads it with arrow, so no DBN decoder is needed. The data
is identical; see \code{vignette("equivalence")} for the column-by-column
comparison and the handful of documented differences.
}
\details{
\strong{This endpoint costs money}, billed per gigabyte. Call \code{\link[=db_get_cost]{db_get_cost()}}
with the same arguments first; previews are free. For the same reason this
is the only request databentoR never retries: a retried stream can be
billed twice.

Column types are assigned explicitly from the field name, never inferred.
Inference is unsafe here: a \code{trades} slice whose \code{action} column is all
\code{"T"} would otherwise be read as boolean. See \code{\link[=db_field_types]{db_field_types()}}.
}
\examples{
\dontrun{
# always preview the cost first
db_get_cost("GLBX.MDP3", start = "2024-01-01", end = "2024-02-01",
            symbols = "ES.FUT", schema = "ohlcv-1d", stype_in = "parent")

es <- db_get_range("GLBX.MDP3", start = "2024-01-01", end = "2024-02-01",
                   symbols = "ES.FUT", schema = "ohlcv-1d",
                   stype_in = "parent")
}
}
\concept{timeseries}
