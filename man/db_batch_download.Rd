% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{db_batch_download}
\alias{db_batch_download}
\title{Download the output of a batch job}
\usage{
db_batch_download(
  job_id,
  output_dir = ".",
  filename_to_download = NULL,
  keep_zip = FALSE
)
}
\arguments{
\item{job_id}{Job identifier, as returned by \code{\link[=db_batch_submit_job]{db_batch_submit_job()}}.}

\item{output_dir}{Directory to write into; a subdirectory named after the
job is created inside it. Defaults to the working directory.}

\item{filename_to_download}{Optional single file from the job manifest.}

\item{keep_zip}{Keep the archive after unpacking it. Only meaningful when
downloading a whole job.}
}
\value{
A character vector of the paths written, invisibly.
}
\description{
Mirrors \code{batch.download}. With \code{filename_to_download = NULL} the whole job
is fetched as one zip archive and unpacked; naming a single file fetches
just that file from the job manifest.
}
\details{
A file that already exists locally at the size the manifest reports is left
alone, so an interrupted download resumes by calling the function again.
When the \code{digest} package is installed, each file's SHA-256 is checked
against the manifest and a mismatch raises a warning.
}
\examples{
\dontrun{
db_batch_download("GLBX-20240101-ABCDEF", output_dir = tempdir())
}
}
\seealso{
Other batch:
\code{\link[=db_batch_get_job_details]{db_batch_get_job_details()}},
\code{\link[=db_batch_list_files]{db_batch_list_files()}},
\code{\link[=db_batch_list_jobs]{db_batch_list_jobs()}},
\code{\link[=db_batch_submit_job]{db_batch_submit_job()}}
}
\concept{batch}
