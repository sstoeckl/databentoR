% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{db_batch_submit_job}
\alias{db_batch_submit_job}
\title{Submit a batch job}
\usage{
db_batch_submit_job(
  dataset,
  symbols,
  schema,
  start,
  end = NULL,
  encoding = "dbn",
  compression = "zstd",
  pretty_px = FALSE,
  pretty_ts = FALSE,
  map_symbols = NULL,
  split_symbols = FALSE,
  split_duration = "day",
  split_size = NULL,
  delivery = "download",
  stype_in = "raw_symbol",
  stype_out = "instrument_id",
  limit = NULL
)
}
\arguments{
\item{dataset}{Dataset code, e.g. \code{"GLBX.MDP3"}.}

\item{symbols}{Character vector of symbols, a single comma-separated
string, or \code{NULL} for all symbols. With \code{stype_in = "instrument_id"} a
numeric vector is accepted. At most 2000 symbols per request.}

\item{schema}{Schema name, see \code{\link[=db_schemas]{db_schemas()}}.}

\item{start, end}{Request window, start inclusive and end exclusive. A
string passes through verbatim, a \code{Date} becomes a plain date, a
\code{POSIXct} an ISO-8601 UTC instant, and a number is read as nanoseconds
since the UNIX epoch. Leaving \code{end} as \code{NULL} asks the server to
forward-fill from \code{start}.}

\item{encoding}{Output encoding, see \code{\link[=db_encodings]{db_encodings()}}.}

\item{compression}{Output compression, see \code{\link[=db_compressions]{db_compressions()}}.}

\item{pretty_px}{Write decimal prices instead of fixed-point integers.}

\item{pretty_ts}{Write ISO-8601 timestamps instead of nanosecond counts.}

\item{map_symbols}{Append a \code{symbol} field to every record. \code{NULL}, the
default, means \code{TRUE} for the text encodings and \code{FALSE} for DBN.}

\item{split_symbols}{Split the output into one file per symbol.}

\item{split_duration}{Split the output by \code{"day"}, \code{"week"}, \code{"month"},
\code{"year"} or \code{"none"}.}

\item{split_size}{Optional maximum size per output file, in bytes.}

\item{delivery}{Delivery mechanism; \code{"download"} is the only one offered.}

\item{stype_in}{Symbol type of \code{symbols}, see \code{\link[=db_stypes]{db_stypes()}}. Use
\code{"parent"} for product-level symbols such as \code{"ES.FUT"} or \code{"SPX.OPT"}.}

\item{stype_out}{Symbol type of the output, see \code{\link[=db_stypes]{db_stypes()}}.}

\item{limit}{Optional cap on the number of records.}
}
\value{
A one-row tibble of the job's properties, including its \code{id}.
}
\description{
Mirrors \code{batch.submit_job}. Batch jobs are the way to request slices too
large to stream with \code{\link[=db_get_range]{db_get_range()}} — Databento recommends them above
about five gigabytes. Submit the job, poll it with \code{\link[=db_batch_list_jobs]{db_batch_list_jobs()}},
then fetch the output with \code{\link[=db_batch_download]{db_batch_download()}}.
}
\details{
Defaults follow the Python client, so \code{encoding} is \code{"dbn"}. Note that
databentoR can read back only the \code{"csv"} and \code{"json"} encodings; a DBN job
downloads fine but needs a DBN decoder to open.
}
\examples{
\dontrun{
job <- db_batch_submit_job("OPRA.PILLAR", symbols = "SPX.OPT",
                           schema = "ohlcv-1d", start = "2024-01-01",
                           end = "2024-02-01", encoding = "csv",
                           stype_in = "parent")
job$id
}
}
\seealso{
Other batch:
\code{\link[=db_batch_download]{db_batch_download()}},
\code{\link[=db_batch_get_job_details]{db_batch_get_job_details()}},
\code{\link[=db_batch_list_files]{db_batch_list_files()}},
\code{\link[=db_batch_list_jobs]{db_batch_list_jobs()}}
}
\concept{batch}
